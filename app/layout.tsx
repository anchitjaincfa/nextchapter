import type {Metadata} from "next";import "./globals.css";export const metadata:Metadata={title:"NextChapter — prepare for the conversation",description:"A private, calm preparation experience."};export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}